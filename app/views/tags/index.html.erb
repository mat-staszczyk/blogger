<h1>Tags list</h1>

<ul>
<% @tags.each do |tag| %>
  <li> <%= link_to tag.name, tag_path(tag) %> | <%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Do you really want to remove this tag?"} %> </li>
<% end %>

<h5><%= link_to "<< Back to Articles list", articles_path %></h5>
